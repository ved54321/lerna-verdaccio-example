{
  "name": "lerna-verdaccio-example",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna bootstrap",
    "test": "lerna test",
    "registry": "lerna publish --registry http://localhost:4873"
  },
  "devDependencies": {
    "lerna": "^3.16.5"
  }
}
